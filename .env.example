# Protects the automatic-sync endpoint. Generate a long random value for production.
CRON_SECRET=replace-with-a-long-random-secret

# Optional: use a persistent absolute location in production.
# DISPATCH_DESK_DATABASE_PATH=/var/lib/dispatch-desk/dispatch-desk.db
